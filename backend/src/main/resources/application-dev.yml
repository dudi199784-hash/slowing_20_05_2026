server:
  port: 8090
spring:
  thymeleaf:
    cache: false
  output:
    ansi:
      enabled: always
  datasource:
    url: jdbc:h2:mem:db_slowind;MODE=MYSQL
    username: doen
    password:
    driver-class-name: org.h2.Driver
  h2:
    console:
      enabled: true
      path: /h2-console
  jpa:
    hibernate:
      ddl-auto: update
custom:
  fileDirPath: /usr/file
app:
  upload:
    root: ${user.dir}/uploads
